# Java 智能体项目

基于 **LangChain4J** 实现的 Java 智能体示例工程，旨在用更工程化的方式快速搭建「可对话、可扩展、可接入工具/知识库」的 Agent。:contentReference[oaicite:1]{index=1}

---

## ✨ 功能特性

- **LangChain4J 驱动的对话式智能体**：封装模型调用、消息组织与响应输出
- **可扩展的能力编排**：便于新增工具、记忆、RAG、结构化输出等模块
- **Maven 工程化结构**：开箱即用（本地运行 / 打包运行）
- **示例优先**：以“能跑起来、可改造”为目标

---

## 🧱 技术栈

- Java（建议 JDK 17+）
- Maven
- LangChain4J :contentReference[oaicite:2]{index=2}

---

## 🚀 快速开始

### 1）克隆项目

```bash
git clone https://github.com/kongtou-1/XiaoZhi.git
cd XiaoZhi

### 2）配置依赖与大模型信息
项目依赖通过 pom.xml 管理，核心依赖已内置 LangChain4J 相关组件。若需使用特定大模型（如 OpenAI），需在配置文件中添加 API 密钥等信息：
# 大模型配置（以OpenAI为例）
langchain4j.openai.api-key=your-api-key
langchain4j.openai.model-name=gpt-3.5-turbo
langchain4j.openai.temperature=0.7

# 对话记忆配置（默认使用内存存储）
langchain4j.memory.max-messages=50

